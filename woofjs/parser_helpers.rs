// Helper functions for the LALRPOP parser
use crate::token::Token;

pub fn parse_number(token: &Token) -> i64 {
    // For now, we'll need to extract the number from the token slice
    // This is a placeholder - in reality, you'd parse the string slice
    0
}

pub fn extract_identifier(token: &Token) -> String {
    // Placeholder - extract identifier name from token
    String::new()
}

